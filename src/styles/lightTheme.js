import defaultColors from "./defaultColors"

const lightTheme = {
  background: "#FFF",
  textColor: "#222",
  primary: "#8a5bff",
  reverseBackground: "#282a36",
  font: defaultColors.font,
}

export default lightTheme
